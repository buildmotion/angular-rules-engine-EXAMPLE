import { Injectable } from '@angular/core';

@Injectable()
export class DoHttpService {

  constructor() { }

  /**
   * Use this function to emulate a web api call to save information.
   */
  public saveSomething(count: number){}
}
